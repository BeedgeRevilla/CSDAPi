WHERE EXISTS
				(
					SELECT * FROM 
						(SELECT PARTY_RK FROM PARTY_IDENTIFIER	WHERE 
								ID_DOC_TYPE_CD='SECURITYNO'
								AND SRCH_ID_DOC_NO LIKE UPPER(REGEXP_REPLACE((REVERSE('%nationalId%')), '[^0-9a-zA-Z]', '')||'%') /*VALUE FOR NATIONAL ID*/ 
						) PI
					WHERE PA.PARTY_RK=PI.PARTY_RK
				)
