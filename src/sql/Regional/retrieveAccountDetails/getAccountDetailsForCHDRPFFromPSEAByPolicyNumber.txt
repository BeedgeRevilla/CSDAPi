SELECT 
CHDRNUM,
COWNNUM,
PAYRNUM,
MANDREF

FROM 
(
	SELECT CHDR.*,
	ROW_NUMBER() OVER ( PARTITION BY CHDRNUM,CHDRCOY ORDER BY TRANNO DESC ) AS VALID_REC
	FROM
	MR_CHDRPF CHDR
	WHERE CHDRNUM='%policyNumber%' /*VALUE OF POLICY NUMBER*/
	AND VALIDFLAG='1'

)
WHERE VALID_REC=1
